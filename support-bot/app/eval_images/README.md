# Тест-картинки для эвала

Сюда кладите скриншоты, на которые ссылаются кейсы в `eval_cases.json` (поле `image`).
Имя файла = значение `image` в кейсе (например `rashod_edit.png`).
Картинки небольшие, едут в git (это воспроизводимый тест-набор).
